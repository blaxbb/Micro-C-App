<Page
    x:Class="MicroCBuilder.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicroCBuilder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <NavigationView
        x:Name="Navigation"
        ItemInvoked="NavigationView_ItemInvoked"
        BackRequested="NavigationView_BackRequested"
        IsBackEnabled="{Binding ElementName=ContentFrame, Path=CanGoBack}">
        <NavigationView.MenuItemContainerStyle>
            <Style TargetType="NavigationViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0" />
            </Style>
        </NavigationView.MenuItemContainerStyle>
        <NavigationView.MenuItems>
            <NavigationViewItem Content="System Build" Tag="BuildPage">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Repair"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>
        <Grid>
            <Frame Grid.Column="0" x:Name="ContentFrame"/>
            <Grid Grid.Column="0" x:Name="CacheProgressContainer">
                <Grid Grid.Column="0" Background="Black" Opacity=".5"/>
                <StackPanel Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <TextBlock Text="Populating item cache" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <ProgressBar x:Name="CacheProgress" Margin="20"/>
                </StackPanel>
            </Grid>
        </Grid>
    </NavigationView>
</Page>
