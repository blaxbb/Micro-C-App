<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:micro_c_app.ViewModels"
             x:Class="micro_c_app.Views.BuildComponentPage">
    <ContentPage.BindingContext>
        <vm:BuildComponentViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Margin="10">
            <Label Text="Build component selection"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Component.Type}"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Component.ComponentLabel}"
                HorizontalOptions="CenterAndExpand" />
            <Entry Text="{Binding Component.SKU}" Placeholder="Enter SKU"/>
            <Button Command="{Binding SubmitButton}" Text="Submit"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>