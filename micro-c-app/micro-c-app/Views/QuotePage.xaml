<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:micro_c_app.ViewModels"
             xmlns:views="clr-namespace:micro_c_app.Views"
             x:Class="micro_c_app.Views.QuotePage"
             >
    <ContentPage.BindingContext>
        <vm:QuotePageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Send" Order="Secondary" Command="{Binding SendQuote}"/>
        <ToolbarItem Text="Reset" Order="Secondary" Command="{Binding Reset}"/>
        <!--<ToolbarItem Text="Export" Order="Secondary" Command="{Binding ExportQuote}"/>
        <ToolbarItem Text="Import" Order="Secondary" Command="{Binding ImportQuote}"/>-->
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Margin="10,5,10,0" Orientation="Horizontal" x:Name="FlipStack" IsEnabled="{Binding NotBusy}">
            <StackLayout VerticalOptions="FillAndExpand">
                <ListView x:Name="listView" ItemsSource="{Binding Items}" ItemTapped="listView_ItemTapped" SelectionMode="Single" Grid.Row="0" VerticalOptions="FillAndExpand" HasUnevenRows="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Vertical" x:Name="item" Margin="5">
                                    <StackLayout Orientation="Horizontal" Padding="0,0,10,0"  HeightRequest="70">
                                        <StackLayout Orientation="Vertical"  HorizontalOptions="StartAndExpand" VerticalOptions="Center">
                                            <Label Text="{Binding Name}" LineBreakMode="TailTruncation"/>
                                            <StackLayout Orientation="Horizontal">
                                                <Label Text="{Binding Price, StringFormat='${0:#,#0.00}'}" HorizontalOptions="StartAndExpand"/>
                                                <Label Text="{Binding SKU}" HorizontalOptions="EndAndExpand"/>
                                            </StackLayout>
                                            <StackLayout Orientation="Horizontal">
                                                <Label Text="{Binding Specs[UPC]}" HorizontalOptions="StartAndExpand"/>
                                                <Label Text="{Binding Stock}" HorizontalOptions="EndAndExpand"/>
                                            </StackLayout>
                                        </StackLayout>
                                        <Label Text="{Binding Quantity, StringFormat='Qty: {0}'}" MinimumWidthRequest="50" VerticalTextAlignment="Center" Margin="10,0,0,0"/>
                                    </StackLayout>
                                    <FlexLayout Direction="Row" AlignItems="Center" JustifyContent="SpaceEvenly" IsVisible="{Binding Source={x:Reference listView}, Path=SelectedItem, Converter={StaticResource EqualsConverter }, ConverterParameter={Reference item}}">
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="Adjust Price" VerticalTextAlignment="Center"/>
                                            <Entry Text="{Binding Price}" TextChanged="ItemPriceChanged" Keyboard="Numeric" WidthRequest="100"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <Button Text="-" WidthRequest="60" Command="{Binding DecreaseQuantity, Source={RelativeSource AncestorType={x:Type vm:QuotePageViewModel}}}" CommandParameter="{Binding .}"/>
                                            <Label Text="{Binding Quantity, StringFormat='Qty: {0}'}" MinimumWidthRequest="50" VerticalTextAlignment="Center"/>
                                            <Button Text="+" WidthRequest="60" Command="{Binding IncreaseQuantity, Source={RelativeSource AncestorType={x:Type vm:QuotePageViewModel}}}" CommandParameter="{Binding .}"/>
                                        </StackLayout>
                                        <Button Text="X" Command="{Binding RemoveItem, Source={RelativeSource AncestorType={x:Type vm:QuotePageViewModel}}}" CommandParameter="{Binding .}" BackgroundColor="Red" WidthRequest="30"/>
                                    </FlexLayout>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
            <StackLayout x:Name="SecondaryStack" Orientation="Vertical"  HorizontalOptions="FillAndExpand" VerticalOptions="End">
                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Label Text="{Binding Subtotal, StringFormat='Subtotal ${0:#,#0.00}', FallbackValue=$123.45}" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand"/>
                    <Label Text="{Binding TaxedTotal, StringFormat='Total {0}', FallbackValue=(7.5%) $123.45}" HorizontalOptions="EndAndExpand" VerticalOptions="CenterAndExpand"/>
                </StackLayout>
                <views:SearchView x:Name="SearchView" VerticalOptions="FillAndExpand" ProductFound="{Binding OnProductFound}" Error="{Binding OnProductError}" Margin="0,0,0,20" AutoPopSearchPage="True" Orientation="Vertical"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>